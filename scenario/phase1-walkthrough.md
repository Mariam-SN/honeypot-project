# Phase 1 – Attacker Walkthrough

This phase establishes an external foothold via a deceptive corporate web portal.
The attacker gains authenticated access, discovers internal configuration artifacts,
and is motivated to pivot toward internal HR systems.

